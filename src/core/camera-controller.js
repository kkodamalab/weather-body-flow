export class CameraController{constructor(canvas){this.canvas=canvas;this.reset();this.bind()}reset(){this.yaw=0;this.pitch=0;this.panX=0;this.panY=0;this.dolly=0}bind(){let d;this.canvas.addEventListener('contextmenu',e=>e.preventDefault());this.canvas.addEventListener('pointerdown',e=>{d={x:e.clientX,y:e.clientY,right:e.button===2};this.canvas.setPointerCapture(e.pointerId)});this.canvas.addEventListener('pointermove',e=>{if(!d)return;let x=e.clientX-d.x,y=e.clientY-d.y;d.x=e.clientX;d.y=e.clientY;if(d.right){this.panX+=x*.006;this.panY-=y*.006}else{this.yaw+=x*.006;this.pitch=Math.max(-1.45,Math.min(1.45,this.pitch+y*.006))}});this.canvas.addEventListener('pointerup',()=>d=null);this.canvas.addEventListener('wheel',e=>{e.preventDefault();this.dolly=Math.max(-12,Math.min(12,this.dolly+e.deltaY*.012))},{passive:false})}transform(p){let cy=Math.cos(this.yaw),sy=Math.sin(this.yaw),cp=Math.cos(this.pitch),sp=Math.sin(this.pitch),x=p.x*cy-p.z*sy,y=p.y,z=p.x*sy+p.z*cy;return{x:x+this.panX,y:y*cp-z*sp+this.panY,z:y*sp+z*cp-this.dolly}}}
