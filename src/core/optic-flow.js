export class OpticFlowRenderer{constructor(canvas,engine,camera,config){this.canvas=canvas;this.ctx=canvas.getContext('2d');this.engine=engine;this.camera=camera;this.config=config}resize(){let w=Math.floor(innerWidth*devicePixelRatio),h=Math.floor(innerHeight*devicePixelRatio);if(this.canvas.width!==w||this.canvas.height!==h){this.canvas.width=w;this.canvas.height=h}}render(){this.resize();let c=this.config(),x=this.ctx;x.fillStyle=c.background;x.fillRect(0,0,c.width,c.height);x.fillStyle=c.color;x.globalAlpha=c.opacity;x.shadowColor=c.color;x.shadowBlur=c.glow;let shown=0;for(const p of this.engine.particles){let q=this.camera.transform(p);if(q.z<=c.near*.15)continue;let sx=c.foeX*c.width+c.focal*q.x/q.z,sy=c.foeY*c.height+c.focal*q.y/q.z;if(sx<-5||sx>c.width+5||sy<-5||sy>c.height+5)continue;let r=Math.max(.3,Math.min(c.size*2,c.size*(c.far/q.z)*.13));x.beginPath();x.arc(sx,sy,r,0,Math.PI*2);x.fill();shown++}x.globalAlpha=1;x.shadowBlur=0;return shown}}
