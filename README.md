# Weather / Body Flow — rebuild-v2

STEP 1 is a standalone Canvas 2D 3D optic-flow renderer. It has no Camera,
MediaPipe, GPS, weather, or external-library dependency. Open `index.html` via
a local HTTP server, then use drag, right drag, and wheel to control the view.
